# คู่มือการใช้งาน Team Planner

Team Planner ใช้สำหรับวางแผนงาน ติดตามงาน และอนุมัติแผนงานของทีม ข้อมูลจะถูกบันทึกลงฐานข้อมูลที่เชื่อมต่อกับระบบโดยอัตโนมัติ

## 1. การเข้าสู่ระบบ

1. เลือกบัญชีผู้ใช้จากรายการบนหน้าเข้าสู่ระบบ
2. กรอกรหัสผ่าน
3. เลือก `จดจำรหัสผ่านบนอุปกรณ์นี้` เมื่อต้องการให้ระบบจดจำบัญชีบนอุปกรณ์ปัจจุบัน
4. กด `เข้าสู่ระบบ`

บัญชีที่ถูกปิดใช้งานจะไม่ปรากฏในรายการเข้าสู่ระบบ เมื่อใช้งานเสร็จ ให้เลือก `ออกจากระบบ` จากเมนูด้านซ้าย

## 2. สิทธิ์ผู้ใช้งาน

| บทบาท | ความสามารถหลัก |
| --- | --- |
| Admin | ดู Dashboard, จัดการผู้ใช้, ดูสถานะทีมทั้งหมด, ดูปฏิทิน, รายการแผน และประวัติการอนุมัติ |
| Manager | สร้างแผนของตนเอง, ดูและติดตามแผนของ Supervisor ในทีม, อนุมัติหรือส่งกลับแผน, ดูปฏิทินและประวัติ |
| Supervisor | สร้างและส่งแผนของตนเองเพื่อรอการอนุมัติ, ติดตามแผนของตนเอง, ดูปฏิทินและประวัติ |

## 3. สร้างและส่งแผนงาน

1. เลือกเมนู `Plans` หรือ `My Plan`
2. ระบุวันที่ เลือกระดับความสำคัญ และกรอกรายละเอียดในช่อง `แผนงาน`
3. กดปุ่มบันทึก

Supervisor จะเห็นปุ่ม `ส่งแผนเพื่ออนุมัติ` และแผนจะมีสถานะ `Pending` จนกว่า Manager จะดำเนินการ ส่วนแผนที่ Manager สร้างจะมีสถานะ `Approved` ทันที

ในตารางแผนงาน ผู้สร้างแผนสามารถลบแผนของตนเองได้ เมื่อแผนได้รับอนุมัติแล้ว ผู้สร้างสามารถเปลี่ยนสถานะเป็นทำเสร็จได้

## 4. อนุมัติแผนงาน (Manager)

1. เลือกเมนู `Approval Center`
2. ตรวจสอบชื่อผู้ส่ง วันที่ รายละเอียดงาน และระดับความสำคัญ
3. เลือกการดำเนินการที่ต้องการ

| การดำเนินการ | ผลลัพธ์ |
| --- | --- |
| `อนุมัติ` | เปลี่ยนสถานะเป็น `Approved` และบันทึกชื่อผู้อนุมัติ |
| `Reply` | ส่งข้อความให้ผู้ส่งแผนเพื่อแก้ไขหรือให้ข้อมูลเพิ่มเติม สถานะเป็น `Replied` |
| `ยกเลิก` | ระบุเหตุผลยกเลิก สถานะเป็น `Cancelled` |

ทุกการดำเนินการในหน้านี้จะถูกบันทึกไว้ใน `Approval History`

## 5. ใช้ปฏิทินทีม

เลือกเมนู `Calendar` เพื่อดูแผนงานที่ได้รับอนุมัติแล้ว

- ใช้ปุ่ม `เดือน` หรือ `สัปดาห์` เพื่อเปลี่ยนรูปแบบการแสดงผล
- ใช้ปุ่มเดือนก่อน/ถัดไป หรือสัปดาห์ก่อน/ถัดไป เพื่อดูช่วงเวลาอื่น
- Manager เลือกดูแผนของ Supervisor รายบุคคล หรือเลือกพนักงานทั้งหมดได้
- กด `ดูรายละเอียด` บนรายการงานเพื่อดูผู้วางแผน สถานะ ผู้อนุมัติ และผลการทำงาน
- ผู้สร้างแผนที่ได้รับอนุมัติ กด `ทำเสร็จแล้ว` และกรอกรายละเอียดผลการทำงานก่อนยืนยันได้
- กด `Capture` เพื่อดาวน์โหลดภาพปฏิทินเป็นไฟล์ PNG
- กด `Export Excel` เพื่อดาวน์โหลดรายการแผนที่อนุมัติแล้วตามตัวกรองปัจจุบันเป็นไฟล์ Excel

## 6. ติดตามสถานะทีม

Manager และ Admin ใช้เมนู `Team Status` เพื่อติดตามแผนรายสัปดาห์

1. เลือกวันเริ่มต้นสัปดาห์
2. ตรวจดูจำนวนแผนทั้งหมด จำนวนที่อนุมัติแล้ว และจำนวนที่รออนุมัติ
3. ตรวจดูสถานะของแต่ละคนในตาราง

Manager จะเห็นเฉพาะ Supervisor ที่อยู่ภายใต้การดูแลของตน ส่วน Admin เห็นรายชื่อผู้ใช้ทั้งหมด

## 7. จัดการผู้ใช้ (Admin)

เลือกเมนู `Admin Management` เพื่อจัดการบัญชีผู้ใช้

### เพิ่มผู้ใช้

1. กรอก Employee ID, ชื่อ-นามสกุล และแผนก
2. เลือกบทบาท `Manager`, `Supervisor` หรือ `Admin`
3. สำหรับ Supervisor ให้เลือก Manager ที่ดูแล
4. กด `เพิ่มพนักงาน`

ผู้ใช้ใหม่มีรหัสผ่านเริ่มต้นเป็น `1234` ควรเปลี่ยนรหัสผ่านหลังเริ่มใช้งาน

### จัดการผู้ใช้เดิม

- `เปลี่ยนผู้บังคับบัญชา`: เปลี่ยน Manager ของ Supervisor โดยระบุ ID ของ Manager
- `ปิดใช้งาน` / `เปิดใช้งาน`: ควบคุมว่าบัญชีจะแสดงในหน้าเข้าสู่ระบบหรือไม่
- `ตั้งรหัสผ่าน`: กำหนดรหัสผ่านใหม่ให้ผู้ใช้
- `ลบ`: ลบบัญชีและแผนงานทั้งหมดของบัญชีนั้นอย่างถาวร

## 8. ดูภาพรวมและประวัติ

- `Dashboard` แสดงจำนวนพนักงาน จำนวนผู้ส่งแผน รายการรออนุมัติ และรายการที่อนุมัติแล้ว
- `Approval History` แสดงเวลา การดำเนินการ ผู้ดำเนินการ และข้อความประกอบของประวัติการอนุมัติ

## 9. ความหมายของสถานะและความสำคัญ

| รายการ | ความหมาย |
| --- | --- |
| `Pending` | รอ Manager อนุมัติ |
| `Approved` | อนุมัติแล้วและแสดงในปฏิทิน |
| `Replied` | Manager ส่งกลับเพื่อขอข้อมูลหรือการแก้ไข |
| `Cancelled` | Manager ยกเลิกแผน |
| `Normal` | ความสำคัญปกติ |
| `High` | ความสำคัญสูง |
| `Low` | ความสำคัญต่ำ |

## 10. ข้อควรทราบ

- ระบบบันทึกการเปลี่ยนแปลงโดยอัตโนมัติหลังแก้ไขข้อมูลไม่นาน
- หากขึ้นข้อความว่าไม่สามารถโหลดหรือบันทึกข้อมูลได้ ให้ตรวจสอบการเชื่อมต่ออินเทอร์เน็ตและการตั้งค่าฐานข้อมูลของระบบ
- การจดจำรหัสผ่านใช้ได้เฉพาะเบราว์เซอร์และอุปกรณ์ที่เลือกไว้ จึงไม่ควรเปิดใช้บนอุปกรณ์สาธารณะ